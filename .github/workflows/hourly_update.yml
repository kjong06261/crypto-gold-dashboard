name: Update Market Data

on:
  schedule:
    - cron: '0 * * * *'  # 매시간 실행
  workflow_dispatch:  # 수동 실행 가능

jobs:
  update:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # 🔥 이거 추가 필수!
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install yfinance pandas
      
      - name: Run update script
        run: |
          python update_data.py
```

---

## 📂 **파일 위치 (정확)**
```
crypto-gold-dashboard/
├── .github/
│   └── workflows/
│       └── update.yml          # 워크플로우
├── update_data.py              # 🔥 이 artifact 코드
├── coin.html                   # 자동 생성됨
├── dividend.html               # 자동 생성됨
├── index.html                  # 자동 생성됨
├── status.json                 # 자동 생성됨
└── README.md
