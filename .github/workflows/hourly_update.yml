name: Update Asset Data

on:
  schedule:
    - cron: "0 * * * *"  # 매시간 정각 실행
  workflow_dispatch:      # 수동 실행 버튼 활성화

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          pip install yfinance pandas requests schedule lxml

      - name: Run Update Script
        run: python update_data.py once  # 엔터프라이즈 코드를 위해 'once' 추가

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          # 모든 html 파일과 데이터/로그를 싹 다 저장합니다.
          git add index.html coin.html dividend.html status.json trends.json logs/ backups/
          git commit -m "🚀 Enterprise Data Update: $(date +'%Y-%m-%d %H:%M KST')" || echo "No changes to commit"
          git push origin main
